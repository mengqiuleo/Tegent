---
description: 审查指定文件或当前 git 改动，输出问题清单
---

审查范围：$ARGUMENTS

- 指定了文件或目录时，审查它们当前的内容。
- 没有指定范围时，先运行 `git diff` 查看未提交改动；没有未提交改动就用 `git diff HEAD~1` 审查最近一次提交。

审查要求：

1. 按严重程度从高到低排列发现：正确性缺陷 > 设计问题 > 可简化点。
2. 每条发现包含文件路径:行号、问题说明、建议的修复方式。
3. 只输出审查报告，不修改任何文件。
