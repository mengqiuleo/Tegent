/** 主强调色——Claude 品牌橙（`claude = rgb(215,119,87)`） */
export const ACCENT = '#d77757'

/** 弱化强调色——中灰，用于状态栏里的次要标签 */
export const ACCENT_DIM = '#999999'

/** 系统 spinner 蓝（`claudeBlue_FOR_SYSTEM_SPINNER = rgb(147,165,255)`） */
export const SPINNER_BLUE = '#93a5ff'

/** 浅蓝紫——权限弹窗、建议、高亮（`permission = rgb(153,204,255)`） */
export const BLUE_PURPLE = '#99ccff'

/** 成功 / 已完成 / diff 新增（`success = rgb(78,186,101)`） */
export const SUCCESS = '#4eba65'

/** 警告 / 权限确认 / 等待中（`warning = rgb(255,193,7)`） */
export const WARNING = '#ffc107'

/** 错误 / 已拒绝 / diff 删除（`error = rgb(255,107,128)`） */
export const ERROR = '#ff6b80'

/** 弱化元素——使用 ANSI 命名色 gray 以获得最广的兼容性 */
export const DIM = 'gray'

/** 更暗的灰色，用于边框/背景（`subtle = rgb(80,80,80)`） */
export const SUBTLE = '#505050'

/** 输入框上下分隔线（`promptBorder = rgb(136,136,136)`） */
export const PROMPT_BORDER = '#888888'

